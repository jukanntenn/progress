<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #1890ff; color: white; padding: 20px; border-radius: 5px 5px 0 0; }
        .content { background-color: #f9f9f9; padding: 20px; border: 1px solid #e0e0e0; }
        .meta { margin: 0 0 12px 0; }
        .meta strong { color: #666; }
        pre { white-space: pre-wrap; word-wrap: break-word; background: #fff; padding: 12px; border-left: 4px solid #1890ff; }
        a { color: #1890ff; }
        .footer { text-align: center; color: #999; font-size: 12px; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>{{ subject }}</h2>
        </div>
        <div class="content">
            <p class="meta"><strong>Name:</strong> {{ name }}</p>
            <p class="meta"><strong>Version:</strong> {{ version }}</p>
            <p class="meta"><strong>URL:</strong> <a href="{{ url }}">{{ url }}</a></p>
            <pre>{{ description }}</pre>
        </div>
        <div class="footer">
            <p>{{ _("This email was sent automatically by Progress, please do not reply.") }}</p>
        </div>
    </div>
</body>
</html>

