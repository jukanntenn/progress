<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #1890ff; color: white; padding: 20px; border-radius: 5px 5px 0 0; }
        .content { background-color: #f9f9f9; padding: 20px; border: 1px solid #e0e0e0; }
        .summary { margin: 20px 0; padding: 15px; background-color: #fff; border-left: 4px solid #1890ff; }
        .fields { display: flex; justify-content: space-between; margin: 20px 0; }
        .field { flex: 1; text-align: center; }
        .field-label { font-weight: bold; color: #666; }
        .field-value { font-size: 24px; color: #1890ff; margin-top: 5px; }
        .button { text-align: center; margin: 20px 0; }
        .btn { display: inline-block; padding: 12px 24px; background-color: #1890ff; color: white; text-decoration: none; border-radius: 5px; }
        .footer { text-align: center; color: #999; font-size: 12px; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>{{ subject }}</h2>
        </div>
        <div class="content">
            <div class="summary">
                <strong>Change Summary</strong>
                <p>{{ summary }}</p>
            </div>
            <div class="fields">
                <div class="field">
                    <div class="field-label">Commits</div>
                    <div class="field-value">{{ commit_count }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Repository</div>
                    <div class="field-value">{{ repo_name }}</div>
                </div>
            </div>
            {% if markpost_url %}
            <div class="button">
                <a href="{{ markpost_url }}" class="btn">View Detailed Report</a>
            </div>
            {% endif %}
        </div>
        <div class="footer">
            <p>This email was sent automatically by Progress, please do not reply.</p>
        </div>
    </div>
</body>
</html>
